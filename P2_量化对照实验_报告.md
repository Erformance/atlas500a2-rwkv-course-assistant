# P2 报告：修正校准时序后的 int8 对照实验

日期：2026-09-23　对应协议：`02_补实验执行协议.md` 第 4 节（P2-A / P2-F）
前提：P0 四项已通过（见 `P0_校准与参考一致性_报告.md`）

## 1. 结论（一句话）

**校准采样的时序错误是此前"int8 崩坏"的主因。** 修正后，同样 2.06 倍速度下：
top-1 一致率从 **0% → 67.2%**，logits KL 从 **10.98 → 0.945**（降低 11.6 倍），
生成文本从"乱码/英文模板"变成"连贯但略有退化"。

残余退化仍然存在，来源是工具链的离线静态 per-tensor 激活量化本身较粗，
与"时序错误"是**两个叠加因素**，不是一个替代另一个。

## 2. 三臂设置

| 臂 | 模型文件 | 校准数据 | 层数 |
| --- | --- | --- | --- |
| fp16（参考） | `layerN.om` + `head.om` | 无 | 0 |
| label-free int8 | `layerN_q.om` + `head_q.om` | 无（工具默认刻度） | 33（32 层 + head） |
| **新校准 int8** | `layerN_cp_q.om` + `head_cp_q.om` | **`calib_pre/`（执行前采样，16 步）** | 33 |

三臂都在同一台设备、同一段固定 token 序列（64 步，取自小试文本）上运行，各自独立进程，
避免设备内存累积影响计时。

## 3. 连续轨迹指标（强制 token 模式）

| 指标 | label-free int8 | **新校准 int8** | 说明 |
| --- | --- | --- | --- |
| logits KL 均值（vs fp16） | **10.982** | **0.945** | 降低 11.6 倍 |
| KL 前 10 步 | 8.742 | 1.812 | |
| KL 后 10 步 | **12.162（上升）** | **0.626（下降）** | 校准臂不发散；label-free 持续漂移 |
| top-1 一致率 | **0.0%** | **67.2%** | 预测目标是否与 fp16 相同 |
| wkv 状态相对偏差 | 0.2009 | **0.0564** | 状态轨迹偏差降 3.6 倍 |

KL 沿序列的走向是本次最有信息量的一点：校准臂的误差**随步数收敛**（1.81 → 0.63），
说明递归状态没有累积性破坏；label-free 则是持续放大（8.74 → 12.16），
对应此前观察到的"越生成越乱"。

## 4. 速度（固定工作量）

| 臂 | prefill（64 token） | decode | 相对 fp16 |
| --- | --- | --- | --- |
| fp16 | 11.12 s | 173.40 ms/token（5.77 tok/s） | 1.00× |
| label-free int8 | 5.34 s | 83.34 ms/token（12.00 tok/s） | **2.08×** |
| 新校准 int8 | 5.38 s | 83.91 ms/token（11.92 tok/s） | **2.06×** |

两臂速度一致（同一套 int8 kernel），因此第 3 节的差异**纯粹来自量化质量**，
不存在"用更多计算换更好结果"的混淆。

## 5. 文本对照（自由生成，贪心解码，80 token）

| 提问 | fp16 | 新校准 int8 | label-free int8 |
| --- | --- | --- | --- |
| 什么是熵？ | 正确完整 | **连贯正确**（略啰嗦） | 崩坏：变成英文 Cookie 政策段落 |
| 写判断素数的 Python 函数 | 完整代码 | 代码写出，**尾部开始复述对话模板** | 乱码（`Hex: GettyImages…`） |

判读：修正后的 int8 **首段回答已可用**；退化出现在长输出与结构化输出（代码）上，
表现为逐步滑向模板复述。这与第 3 节"KL 仍在 0.6~1.8 量级、top-1 一致率 67%"一致——
单步偏差不大，但在自由生成里会通过 token 反馈被放大（协议 P2-F 关注的正是这个差异）。

## 6. 对原结论的修订

此前报告（主报告第十四节）把 int8 失败归因于"工具链缺 W8A16 等手段"。
按 P0/P2 证据修订为：

1. **首要原因是校准数据时序错误**——旧校准数据里的状态量不是该层看到的输入，
   在 layer0 上甚至让误差比 label-free 还差 3 倍（0.944 vs 0.301）；修正后 layer31 的
   单层误差从 1.113 降到 0.450。
2. **次要原因是工具链的静态 per-tensor 激活量化**：修正后仍有 0.45 量级的单层误差、
   67% 的 top-1 一致率，说明要追平 fp16 需要更细的量化粒度（分通道/动态激活刻度），
   而这部分能力 CANN 8.0.RC1 的 ONNX 量化工具确实没有。

比例上：**时序修正贡献了绝大部分改善**（KL 11.0 → 0.95），剩下的是工具链能力边界。

## 7. 交付物

| 交付物 | 位置 |
| --- | --- |
| 逐臂指标汇总 | 设备 `ab_logits.json`、本地 `ab_logits_device.json` |
| 逐臂原始 logits 与状态摘要 | 设备 `arm_fp16.npz`、`arm_q.npz`、`arm_cp_q.npz`（各约 7~8 MB） |
| 臂元信息 | 设备 `meta_fp16.json`、`meta_q.json`、`meta_cp_q.json` |
| 新校准模型 | 设备 `layer{0..31}_cp_q.om`、`head_cp_q.om` |
| 脚本 | `ab_run_arm.py`、`ab_compare.py`、`build_cp_layers.sh`、`capture_calib_pre.py` |

## 8. 下一步（供决策）

按协议第 10 节，最小启动包的四份材料已齐：manifest、P0 重放报告、
新旧采样 A/B、三条基线的速度与连续轨迹指标。据此判断：

**主线（时域指标是否优于局部误差）具备继续探索的价值**——因为现在有了一个
"同一速度、不同质量"的真实候选空间（top-1 一致率 67% vs 0%），
而不是此前"要么 fp16 要么崩坏"的二选一。

建议的下一步优先级：

1. 扩大校准数据（16 → 64~128 段，状态年龄覆盖 0/64/256），看 top-1 一致率能否继续上升；
2. 按协议 P2 的 E 组做路径分解（head-only / FFN-only / recurrent-only），定位残余误差主要来自哪条路径；
3. 若 1、2 有信号，再进入 P3 的时域指标研究；否则把结论写成"工具链能力边界"的分析章节。
