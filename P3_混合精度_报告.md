# P3 报告：逐层敏感度与混合精度帕累托曲线

日期：2026-09-23　对应协议：`02_补实验执行协议.md` 第 4~5 节（P2-E 路径分解 / P3 指标价值）
前提：P0 通过、P2 完成（新校准 int8 全量 KL 0.945 / top-1 67.2%）

## 1. 方法（零编译成本）

fp16 与 int8 两套逐层模型**都已存在于磁盘**，因此可以按层自由组合、无需重新编译：
给引擎加"逐层计划"能力（`plan = {"suffix": "_cp_q", "layers": [...], "head": bool}`），
任何组合都能直接跑。这使混合精度从"理论方案"变成"当天可测的实验"。

两步走：

1. **逐层敏感度**（`sensitivity.py`）：每次只把一层（或输出头）换成 int8，
   在固定 64-token 序列上测与 fp16 的 logits KL / top-1 一致率。33 个目标 ≈ 40 分钟。
2. **帕累托扫描**（`pareto.py`）：把"层"和"输出头"统一视为可量化单元，按 KL 从
   小到大排序，取前 N 个做 int8、其余保 fp16，逐步实测质量与速度。

## 2. 逐层敏感度（33/33）

**最敏感的 6 个目标**：

| 目标 | KL（只量化该层） | top-1 一致率 |
| --- | --- | --- |
| layer9 | 0.441 | 75.0% |
| layer0 | 0.189 | 87.5% |
| layer1 | 0.091 | 89.1% |
| layer21 | 0.082 | 92.2% |
| layer27 | 0.044 | 95.3% |
| layer26 | 0.022 | 98.4% |

**27 个目标 KL < 0.02**（含输出头 0.008、layer2~8、10~20 等），即"量化几乎不花钱"。

分布极不均匀是混合精度可行的前提：只有 5~6 层需要保护，其余 27 层可以放心压。

> 附带修正：此前报告里"仅输出头 int8 → 崩坏"的结论**不再成立**——那是用错误时序的
> 校准数据（label-free）测的；用修正后的校准数据，输出头单独量化的 KL 仅 0.008、
> top-1 一致率 96.9%。

## 3. 帕累托曲线（固定 64-token 序列，贪心取最安全的 N 个单元）

| 计划 | int8 单元 | KL 均值 | top-1 一致率 | decode | 相对 fp16 |
| --- | --- | --- | --- | --- | --- |
| fp16（参考） | 0 | — | 100% | 5.77 tok/s | 1.00× |
| top8 | 8 | **0.019** | **93.8%** | 6.87 tok/s | 1.19× |
| **top16** | 16 | **0.049** | **92.2%** | **7.77 tok/s** | **1.35×** |
| top24 | 24（含 head） | 0.142 | 85.9% | 9.77 tok/s | 1.69× |
| top28 | 28 | 0.262 | 79.7% | 10.65 tok/s | 1.85× |
| top33（全量） | 33 | 0.945 | 67.2% | 12.04 tok/s | 2.09× |

**top16 是质量—速度的甜点**：1.35 倍速度下，KL 只有全量 int8 的 **1/19**，
top-1 一致率 92.2%（对比全量 67.2%）。

## 4. 文本验证（实际生成，80 token，贪心解码）

| 配置 | 速度 | 「什么是熵？」 | 「写判断素数的函数」 |
| --- | --- | --- | --- |
| fp16 | 4.89 tok/s | 正确 | 完整代码 |
| top16 | 6.70 tok/s | **正确连贯** | 代码正确，但尾部复述对话模板 |
| top24 | 8.83 tok/s | 正确且结构化 | 未给代码，转为概念解释（连贯但答非所问） |

**关键发现（有方法论价值）**：短问答在 top16 下质量保持良好，但**代码题在 KL 仅 0.049 时
仍会翻车**。原因是结构化长输出对每个 token 都敏感，8% 的 top-1 分歧会在生成过程中被
token 反馈放大——这正是协议 P2-F（强制 token vs 自由生成）所指的机制。

更值得注意的是：**本轮的敏感度是按自然文本序列测的**，而代码题暴露的失效说明
**敏感度本身依赖数据分布**。这与协议 P1"校准 / validation / test 必须分离、
演示提示不能承担最终评估"的要求完全一致——如果只用问答文本选层，
就会选出一个"问答友好、代码不友好"的计划。

## 5. 结论与可用形态

1. **立即可用**：top16 作为"快速档"用于短问答（1.35×，质量保持）；代码与长输出继续走 fp16。
   这对应协议 P4 里的"固定敏感路径保护"基线。
2. **还可再优化**：用"代码 + 自然文本"混合序列重测敏感度（约 40 分钟），
   得到一个对两类任务都友好的计划；或按任务类型各存一套计划。
3. **与协议主线的衔接**：现在有了"同速度、不同质量"的真实候选空间
   （top8/16/24/28/33 五个点），P3 想验证的"时域指标是否优于局部误差"终于有了
   可比较的实验平台。

## 6. 交付物

| 交付物 | 位置 |
| --- | --- |
| 逐层敏感度 | 设备 `sensitivity.json`（33/33） |
| 帕累托数据 | 设备 `pareto.json` |
| 计划文件 | 设备 `plan_top16.json`、`plan_top24.json` |
| 脚本 | `sensitivity.py`、`pareto.py`、`ab_run_arm.py`、`ab_compare.py` |
| 引擎能力 | `rwkv7_serve2.py` 支持逐层计划；`rwkv7_chat.py` / `rwkv7_http.py` 支持 `--plan-file` |
