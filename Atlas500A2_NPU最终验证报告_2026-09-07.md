# Atlas 500 A2 × RWKV 部署 — 最终验证报告

**验证日期**：2026-09-07（设备时区 UTC，约 10:19–10:50）
**验证对象**：Atlas 500 A2 智能小站（昇腾 310B NPU，aarch64 / EulerOS 2.0 SP11）
**验证目标**：确认“在硬件本地用 NPU 跑通 RWKV-6 World 1B6”当前是否可行，并固化证据链。

---

## 一、结论

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 硬件 / 驱动 | PASS | `npu-smi` 显示 310B，Health OK，驱动 23.0.rc1.1 / rc1.2 |
| CPU 推理 RWKV | PASS | 本次复测 16 token 确定性校验 PASS（两次输出一致） |
| NPU 推理 RWKV（torch_npu） | FAIL | 反复验证均无法完成设备初始化/真实算子执行 |

**总体判定：在当前已安装的 23.0.RC1 驱动与可获取的 CANN/torch_npu 组合下，用 torch_npu 在这台 Atlas 500 A2 上跑 RWKV 未能跑通。失败点在平台软件栈（驱动/CANN/算子支持），不是模型代码问题（CPU 回归已 PASS）。**

---

## 二、本次最终验证实测记录

### 2.1 硬件与驱动状态（PASS）

```text
npu-smi 23.0.rc1.2            Version: 23.0.rc1.1
NPU 0  310B   Health: OK   Power: 8.x W   Temp: ~40 C
/dev/davinci0       存在
/dev/davinci_manager 存在
/dev/davinci_mdio    存在
```

DMP 通信进程本次已按既有流程重新拉起：

```text
/var/slogd                    （运行中）
/var/dmp_daemon -I -U 8087   （运行中）
```

### 2.2 CPU 推理回归（PASS）

命令：

```bash
/home/disk/miniconda3/envs/rwkv/bin/python run_demo.py \
  --device cpu --tokens 16 --verify
```

结果（本次实测）：

```text
模型加载耗时: 19.2 s
[run-1] 16 token, 12.9 s, 1.239 tok/s
[run-2] 16 token, 12.3 s, 1.296 tok/s
两次输出一致: " 傅里叶变换是一种将时域信号转换"
确定性校验: PASS
```

### 2.3 NPU 容器内最小算子测试（FAIL，多轮复测）

最小测试脚本：`/home/disk/rwkv-demo/min_npu_test.py`

```python
import torch, torch_npu
print(torch.npu.is_available(), torch.npu.device_count())
x = torch.ones(4, 4, device='npu')   # 第一行真实算子
y = (x @ x).cpu()
```

| 时间（UTC） | 环境 | 结果 |
| --- | --- | --- |
| 09:41 / 09:43 | verl 容器 + 宿主机 driver 挂载 | `HDC init failed, driver may not load` |
| 10:16–10:17 | verl 容器（保留镜像自带 CANN 8.3.RC1）+ davinci/mdio/bbox 设备节点 | torch_npu 可 import，`is_available=True`；执行 `torch.ones(device='npu')` 时报 `RuntimeError 507033`，日志明确 **`libaicpu_scheduler.so: cannot open shared object file`** |
| 10:25–10:41 | 同上 + 注入宿主机 `/usr/lib64` 的 aicpu/tsd 用户态驱动库（`/opt/driver-extra`） | 出现 `Device is busy`；重启 slogd/dmp_daemon 后仍 `HDC init failed, driver may not load`，进程挂起至 timeout（124） |

关键点：宿主机 `/usr/lib64/libaicpu_scheduler.so`（及其依赖的 aicpu/tsd 库）确实存在，但不在容器挂载的 `/usr/local/Ascend/driver/lib64` 内；补齐后仍无法越过 HDC/设备初始化，说明缺库只是表象之一，根本是驱动与容器内 CANN 8.3 的适配问题。

### 2.4 宿主机直装 torch_npu（FAIL，历史证据）

| 组合 | 结果 |
| --- | --- |
| torch 2.6.0 + torch_npu 2.6.0.post2 + CANN 8.2.RC1 | 初始化失败（详见 `/home/disk/rwkv-demo/npu_op_test80b.log`） |
| torch 2.4.0 + torch_npu 2.4.0.post4 + CANN 8.0.RC1 | `is_available=True`，但执行首个真实算子时因 protobuf ABI 崩溃（`fwk_adapter.pb.cc`），详见 `/home/disk/rwkv-demo/npu_op_test80.log`、`min_npu_test.log` |

宿主机 NNRT 为出厂 8.0.RC1，不含 HCCL；310B 虽有驱动且 `npu-smi` 正常，但 torch_npu 真实算子无法稳定执行。

---

## 三、根因链（按证据强度排序）

1. 设备出厂驱动为 **23.0.RC1.1/rc1.2**；可获得的 verl 容器镜像自带 **CANN 8.3.RC1 + torch 2.7.1 / torch_npu 2.7.1**，但镜像不携带该驱动代次所需的宿主机用户态驱动库，HDC/设备初始化即失败。
2. 宿主机侧自装 CANN 8.0/8.2 + torch_npu 2.4/2.6 属于“跨版本/非官方支持”组合：设备能被识别，但真实算子执行崩溃或不可用。
3. 官方《Atlas 500 A2 智能小站 23.0.0 升级指导书》只覆盖 NPU/MCU/固件到 23.0.RC2 / 23.0.0 的**同代升级**，并不提供可配 CANN 8.x/torch_npu 的更高代驱动，无法解决上述错配。
4. 同系列 310B 上官方跑 LLM 的案例（如 openPangu-Embedded-1B）走 MindIE/ATB + **专为该硬件定制的驱动**（Atlas 200I A2 / OrangePi 路径），未提供 Atlas 500 A2 对应版本。

因此“用 torch_npu 直接跑 RWKV”在当前软件条件下不可行；不等于芯片不可用，而是缺少与 Atlas 500 A2 匹配的官方大模型软件栈。

---

## 四、若要继续 NPU 路线（建议按优先级）

1. 联系华为/昇腾支持，索取 Atlas 500 A2 官方支持的大模型（LLM/MindIE）CANN+驱动组合；这是最正路。
2. 评估 MindIE/ATB + openPangu-Embedded-1B（310B 同芯片、官方有跑通案例）路径，需要额外下载专用驱动与模型，属独立实验。
3. 若项目时间有限，将 **CPU 推理**作为当前可交付成果（已可连续生成、确定性通过），NPU 结论写入结题文档。
4. 再次尝试 NPU 前建议先整机重启一次，以清掉本次多次失败初始化可能遗留的 HDC/驱动状态。

---

## 五、关键证据文件（设备内路径）

```text
/home/disk/rwkv-demo/run_demo.py               # CPU/NPU 推理脚本
/home/disk/rwkv-demo/min_npu_test.py           # NPU 最小算子测试
/home/disk/rwkv-demo/final_cpu_verify.log      # 本次 CPU 回归（PASS）
/home/disk/rwkv-demo/container_npu_test4.log   # 容器内最近一次可 import 但 SetDevice 失败
/home/disk/rwkv-demo/final_rwkv_npu2.log       # 本次补库后仍 HDC 失败
/home/disk/rwkv-demo/npu_op_test80.log         # 宿主机 CANN 8.0 算子崩溃
/home/disk/rwkv-demo/npu_op_test80b.log        # 宿主机 CANN 8.2 失败
/home/disk/models/rwkv-6-world-1b6/            # 1B6 模型（3.0G）
```

---

## 六、验证后设备状态

- NPU：`npu-smi` Health OK；DMP 已重启并运行。
- 容器：`rwkv`（verl_pt27_25rc3:a2-arm）保持运行；临时测试容器已删除。
- 补充材料：宿主机驱动库副本保留在 `/home/disk/driver-extra/`（仅验证用途）。

---

## 七、2026-09-09 整机重启后的补充复测

设备整机重启后复测，取得了新的进展，也把最终失败点收敛到了更窄的一层：

### 7.1 新增实测结果

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| DMP/NPU 通信 | PASS | 重启后仍需手动执行 `/var/slogd` 与 `/var/dmp_daemon -I -U 8087`，`npu-smi` 恢复 310B / Health OK |
| torch_npu 导入 | PASS | rwkv-npu 环境 + source CANN 8.0 toolkit 后可 `import torch_npu`，`is_available=True` |
| CPU→NPU 数据搬运 | PASS | 真实 1B6 权重的连续 `.to('npu:0')` 可完成；单独复现脚本 23 个大/小张量全部搬运成功 |
| RWKV 权重上卡 | 部分 PASS | 修改 rwkv 包加载逻辑（先在 CPU `contiguous()`，再搬 NPU，绕开 NPU 侧 contiguous 触发 TBE 初始化死锁）后，第 0 层权重可完整落到 `npu:0` |
| 真实 matmul | FAIL | fp32 / fp16 matmul 均触发 protobuf ABI 致命错误：CANN 组件编译于 protobuf 3.13.0，运行进程内却检测到 3.21.9（`fwk_adapter.pb.cc`），进程 abort |

### 7.2 关键证据

- 死锁定位：faulthandler + `timeout -s ABRT` 拿到 Python 栈，卡点在 `torch_npu._C._npu_init()` → TBE `repository_manager` 多进程 Manager 初始化（`rwkv/model.py` 首次向 NPU 搬运权重时触发）。
- 绕过死锁后，RWKV 第 0 层（attention + ffn）所有权重都能搬到 `npu:0`，说明“310B 是否接受这张模型”已不再是问题。
- 真正剩余拦截点：torch_npu 真实算子编译/执行路径中，CANN 8.0（自带 3.13 私有 protobuf）与进程内另一份 3.21.9 protobuf 发生 ABI 冲突。这与历史日志 `/home/disk/rwkv-demo/npu_op_test80.log` 中记录的 protobuf ABI 崩溃同源，只是本轮已把触发点从“首个算子”缩小并定位到 TBE 初始化路径。

### 7.3 结论修订

上一版“torch_npu 直接跑 RWKV 不可行”应修订为更精确的表述：

**宿主机 NPU 驱动本身工作正常（310B、npu-smi、设备内存搬运均通过）；失败点是宿主机上自装的 CANN 8.0/8.2 + torch_npu 组合与设备 2023 年代驱动（23.0.RC1）之间的软件栈不一致（protobuf/HDC/算子库），并非芯片不支持。**

华为官网现行固件/驱动页已把 Atlas 200I A2 与 Atlas 500 A2 合并为“200I/500 A2 推理产品”并持续提供新驱动（如 HDK 26.1.1：driver 26.1.0 + firmware 9.0.0.8.200，配套 CANN 8.5.1）。因此下一步最有价值的动作是：按 Atlas 500 A2 官方升级指导书做驱动/固件升级并重装配套 CANN/torch_npu，再复测最小 matmul 与 RWKV。

---

## 八、2026-09-09 版本配套核对与干净 CANN 复测

### 8.1 版本配套核对结论（官方 torch_npu 配套表）

来自 gitee ascend/pytorch README 的官方配套表（非推断）：

| CANN 版本 | PyTorch | torch_npu 插件 | 分支 |
| --- | --- | --- | --- |
| CANN 8.1.RC1 | 2.4.0 | 2.4.0.post4 | v2.4.0-7.0.0 |
| CANN 8.0.0 | 2.4.0 | 2.4.0.post2 | v2.4.0-6.0.0 |
| CANN 8.0.RC3 | 2.4.0 | 2.4.0 | v2.4.0-6.0.rc3 |
| CANN 8.0.RC2 | 2.3.1 / 2.1.0 | 2.3.1 / 2.1.0.post6 | v2.3.1-6.0.rc2 / v2.1.0-6.0.rc2 |
| CANN 8.0.RC1 | 2.2.0 / 2.1.0 | 2.2.0 / 2.1.0.post4 | v2.2.0-6.0.rc1 / v2.1.0-6.0.rc1 |
| CANN 7.0.0 | 2.1.0 | 2.1.0 | v2.1.0-5.0.0 |

此前设备上实际是 **CANN 8.0.RC1 + torch_npu 2.4.0.post4**，而 2.4.0.post4 官方配套 CANN 8.1.RC1，确属版本错配。

### 8.2 干净 CANN 8.0.RC1 + torch_npu 2.2.0 复测（仍 FAIL）

为排除“多版本 CANN 混装污染”，本次将 CANN 8.0.RC1 toolkit 全新安装到独立目录 `/home/disk/cann80base`（与旧 8.0/8.2/8.3 目录完全隔离），新建 Python 3.10 环境 `npu22`，按上表安装 **torch 2.2.0 + torch_npu 2.2.0（cp310 aarch64，gitee v6.0.rc1-pytorch2.2.0 release）**，补齐 scipy 等 TBE 依赖后复测 matmul。

结果：

- `import torch, torch_npu`、`torch.npu.is_available()=True`：PASS
- 首个真实 matmul：**仍触发同样的 protobuf ABI 致命错误**
  `This program was compiled against version 3.13.0 ... not compatible with the installed version (3.21.9) ... (fwk_adapter.pb.cc)`
- CANN 8.0.RC1（NNRT 与 toolkit）自带的 `libascend_protobuf.so.3.13.0.0` 两份副本 md5 完全一致（`c7d04d25...`）。

结论修订：失败不只是“torch_npu/CANN 版本错配”或“本地多版本混装”，而是 **2023 年代驱动 23.0.RC1 所配套的 CANN 8.0.RC1 代软件栈与 torch_npu 算子编译路径存在 protobuf 3.13/3.21 不兼容**，在官方可取的 CANN 8.0.RC1 组合下无法自解。因此按方案 A 升级小站 NPU 驱动/固件到较新官方配套版本（A500 A2 -soc 形态，非 EP 驱动），再装配套 CANN/torch_npu，仍是当前唯一正路。

### 8.3 需要说明的下载陷阱

华为官网现行“200I/500 A2”下载页给出的 `Ascend-hdk-310b-npu-driver_26.1.0_linux-aarch64.run` 与 `Ascend-hdk-310b-npu-firmware_9.0.0.8.200.run` 属于 **Atlas 200I A2 EP 场景（Host 侧）** 包，不含 `-soc`；Atlas 500 A2 智能小站（SoC、预置 EulerOS）需使用 `Ascend-hdk-310b-npu-driver-soc_*` 等带 `-soc` 的软件包，并按《Atlas 500 A2 智能小站 24.0.RC2/24.0.RC3 昇腾软件安装指南》下载与安装。

---

## 九、2026-09-11 关键突破：NPU 推理链路已跑通（ATC + pyACL）

### 9.1 结论

**Atlas 500 A2 的 310B NPU 可以真正执行 AI 推理**，走的不是 torch_npu，而是华为官方支持的推理形态：

```text
PyTorch/ONNX 模型 --ATC--> .om 模型 --pyACL--> 310B NPU 执行
```

整条链路已在宿主机 EulerOS（驱动 23.0.RC1，未升级驱动/固件）实测通过，数值结果与 CPU 参考一致。

### 9.2 实测过程与证据

环境（全部隔离在独立目录，不影响原有安装）：

| 项目 | 值 |
| --- | --- |
| CANN | 8.0.RC1 toolkit，全新安装于 `/home/disk/cann80base` |
| Python | `/home/disk/miniconda3/envs/npu22`（3.10，torch 2.2.0 CPU + numpy 1.26.4 + scipy + onnx） |
| 驱动 | 23.0.RC1（未改动） |
| soc_version | `Ascend310B1` |

步骤与结果：

1. 在设备上用 CPU torch 导出小型全连接网络 ONNX，并保存同一模型在 CPU 上的参考输出：`torch.manual_seed(0)`，输入 `[0,1,2,3,4,5,6,7]`，参考输出 `[-0.324883, -1.1071938, 1.1347766, -1.2253158]`。
2. ATC 编译成 `.om`：`atc --model=tiny.onnx --framework=5 --output=tinyc --soc_version=Ascend310B1 --input_shape="x:1,8"`，回显 `ATC run success`。**ATC 全程没有出现 torch_npu 路径上的 protobuf 3.13/3.21 崩溃，说明该问题只存在于 torch_npu/ATB 路径，CANN 本身编译链路是好的。**
3. pyACL 把 `.om` 加载到 NPU 执行：

```text
[1] acl.init ok
[2] set_device(0) ok
[3] create_context ok
[4] 模型加载 ok，model_id=1
[5] 模型描述: 输入 1 个 / 输出 1 个, 输入 32 字节, 输出 16 字节
[6] 第 1 次执行: NPU 执行 0.186 ms, 含搬运 0.217 ms
[6] 第 2 次执行: NPU 执行 0.166 ms, 含搬运 0.189 ms
[6] 第 3 次执行: NPU 执行 0.092 ms, 含搬运 0.110 ms
输入 : [0. 1. 2. 3. 4. 5. 6. 7.]
NPU输出: [-0.325  -1.1074  1.1357 -1.2256]
CPU参考: [-0.3249 -1.1072  1.1348 -1.2253]
最大误差: 0.000965595，数值比对: PASS
[7] NPU 推理链路验证完成
```

4. 压力验证：5000 次与 100000 次连续执行全部成功，稳态单次 NPU 执行约 0.065 ms。ACL 无 CPU 回退机制，能连续执行且数值正确，说明算子确实在 310B 上完成。

### 9.3 关键文件

```text
/home/disk/cann80base/ascend-toolkit/            # 全新独立 CANN 8.0.RC1
/home/disk/miniconda3/envs/npu22/                # 验证用 Python 环境
/home/disk/tmp/tiny.onnx                         # 测试模型
/home/disk/tmp/tinyc.om                          # ATC 编译产物
/home/disk/tmp/tiny_in.npy / tiny_ref.npy        # 输入与 CPU 参考输出
/home/disk/tmp/atlas500a2_acl_demo.py            # pyACL 推理脚本（工作区有同名副本）
/home/disk/tmp/acl_loop.log, acl_loop2.log       # 5000 / 100000 次执行日志
/home/disk/tmp/atc1.log, atc3.log                # ATC 编译日志
```

### 9.4 对原结论的修订

- 之前“NPU 无法跑 AI”的结论应修订为：**torch_npu 这条软件路径在这台小站上不可用；但 NPU 本身以及官方 ATC+pyACL 推理链路完全可用。**
- 因此项目不需要再去找那些已下架的企业版 `-soc` 驱动/固件包，也不需要冒险升级固件。

### 9.5 下一步（RWKV 上 NPU 的现实路径）

RWKV 要跑在这条 ACL 链路上，需先转 ONNX，再用 ATC 编译成 `.om`，最后用 pyACL 按步喂入状态（RWKV 的 state 作为模型输入/输出）。

- 官方 RWKV-LM 仓库没有 ONNX 导出脚本，HuggingFace 上也没有现成的 RWKV-6 ONNX 模型；RWKV-6 需自行写导出脚本。
- RWKV-4/5 有现成工具与模型（社区仓库 `RWKV/rwkv-onnx` 支持 v5；HuggingFace 上有 RWKV-4 World 0.4B 等 ONNX）。
- 建议先用现成的 RWKV-4/5 小模型打通“RWKV 在 NPU 上出词”的完整链路，再决定是自研 RWKV-6 导出，还是项目改用 RWKV-5。

---

## 十、2026-09-11 里程碑：RWKV 语言模型在 NPU 上跑通

### 10.1 结论

**RWKV 语言模型已经在 Atlas 500 A2 的 310B NPU 上完成真实文本生成**，全流程走官方 ACL 推理链路，未使用 torch_npu，也未改动驱动/固件。

```text
HuggingFace ONNX(RWKV-4 World 0.4B)
   → 两处兼容性修复
   → ATC 编译成 .om（Ascend310B1）
   → pyACL 单步循环（token + 5 个 state 输入 → logits + 5 个 state 输出）
   → NPU 上逐 token 生成中文/英文文本
```

### 10.2 实测结果

模型：`TIEMING/rwkv-world-0.4B-onnx`（RWKV-4 World 0.4B，ONNX 1.65GB，24 层 × 1024 维，词表 65536）

第一次生成（默认 prompt，贪心采样）：

```text
prompt='User: hello\n\nAssistant:' -> 7 个 token
prefill 7 token 用时 0.147 s
  step  0: id=36786 ' Hello'
  step  1: id=34    '!'
  step  2: id=20063 ' How'
  step  3: id=21413 ' can'
  step  4: id=308   ' I'
  step  5: id=45221 ' assist'
  step  6: id=22799 ' you'
  step  7: id=39971 ' today'
  step  8: id=64    '?'
生成文本: ' Hello! How can I assist you today?\n\nUser: 我想了'
生成 16 token 用时 0.363 s（44.03 token/s）
```

第二次生成（换 prompt，验证不是偶然）：

```text
prompt='Q: What is the capital of France? A:' -> 11 个 token
prefill 11 token 用时 0.230 s
  step  0: id=37138 ' Paris'
  step  1: id=47    '.'
生成文本: ' Paris. A: Paris. A: Paris. ...'
生成 24 token 用时 0.507 s（47.37 token/s）
```

两次都能生成连贯、语义正确的文本（第一次给出助手式回复，第二次正确答出法国首都 Paris），说明模型在 NPU 上确实在执行真实的语言建模计算。

### 10.3 过程中解决的三类问题

| 问题 | 现象 | 处理 |
| --- | --- | --- |
| ONNX 携带 9 个 opset domain | ATC 报 `The model has [9] [--domain_version] fields` | 只保留默认 domain（opset 14）另存 |
| `DequantizeLinear` 不被支持 | ATC 报 `No parser is registered for Op [... DequantizeLinear]` | 把 int8 量化的 65536×1024 嵌入表离线反量化为 fp32，改写 Gather 输出名并删除该节点 |
| ATC 编译把设备 CPU 占满导致 SSH 失联 | 编译 2261 节点时 SSH 卡在 banner 超时 | 重启后改用 `TE_PARALLEL_COMPILER=2` + `nice -n 10` 重跑，编译期间设备保持可登录 |

### 10.4 关于 CPU 对照

用 onnxruntime 在同一台设备的 CPU 上跑这份 ONNX（修复版和原始版都试过），得到的词表 logits **全为 NaN**，属于该导出的 CPU 端/runtime 行为问题，与本项目的改动无关，也不影响 NPU 结果。若后续需要严格的数值对照，建议改用原始 `.pth` 权重在 PyTorch 里做参考实现。

### 10.5 关键文件

```text
/home/disk/models/rwkv-onnx-0.4b/model.onnx          # 原始 HF ONNX（1.65GB）
/home/disk/models/rwkv-onnx-0.4b/model_clean.onnx    # 去掉多余 opset 域
/home/disk/models/rwkv-onnx-0.4b/model_fixed.onnx    # 再去掉 DequantizeLinear（1.85GB）
/home/disk/models/rwkv-onnx-0.4b/rwkv04.om           # ATC 编译产物（927MB，fp16）
/home/disk/models/rwkv-onnx-0.4b/rwkv_onnx_npu.py    # ACL/ORT 双后端推理脚本（工作区有副本）
/home/disk/models/rwkv-onnx-0.4b/rwkv_onnx_fix_dequant.py
/home/disk/models/rwkv-onnx-0.4b/acl_rwkv_run.log    # 第一次 NPU 生成日志
/home/disk/models/rwkv-onnx-0.4b/acl_rwkv_q.log      # 第二次 NPU 生成日志
/home/disk/models/rwkv-onnx-0.4b/atc_rwkv04b.log     # 成功的 ATC 编译日志
```

### 10.6 下一步

1. 把生成循环接进课程助手应用（当前脚本已支持 prompt/采样/多轮 token 循环）。
2. 换更大的现成 ONNX（RWKV-5 1B5/3B）验证 NPU 上的显存与吞吐上限；1B6/3B 级别需评估 `.om` 大小与 NPU 内存。
3. 若要保留 RWKV-6 1B6，需要自研 ONNX 导出脚本（把 state 作为输入/输出，单步导出），再走同样的 ATC + pyACL 流程。

---

## 十一、2026-09-11 晚：RWKV-7 2.9B 上 NPU 的工程进展（当日收工快照）

### 11.1 为什么选 2.9B 而不是 7B

- 设备内存 11.5GB 与 CPU 共享，系统占约 3GB。
- 7.2B 的 fp16 权重 14.4GB（已核实 HF 上该模型 15 个分片共 14.4GB）→ **物理上放不下**，必须 int8/4bit 量化，且需要一台 32GB+ 内存的机器做 ATC 编译。
- 2.9B 的 fp16 权重 5.9GB → 放得下，可在设备上完成全部流程。故先做 2.9B 打样，框架与 7.2B 通用。

### 11.2 已完成

| 步骤 | 结果 |
| --- | --- |
| 模型下载 | `RWKV/RWKV7-G1j-2.9B-20260831` 6 个分片（5.9GB）全部下载并**逐字节校验**通过；中途分片 2、5 因并行下载重叠写坏，已重下修正 |
| 运行环境 | 新建 `rwkv7` conda 环境：Python 3.11 + torch 2.14 CPU + transformers 5.17 + safetensors 0.8 + onnx 1.22 |
| 模型加载 | 5.8GB bf16 权重 3.8s 载入；配置：32 层 / hidden 2560 / 40 heads × 64 / FFN 10240 / 词表 65536 |
| CPU 参考值 | 单步前向（prompt「你好」→ ids [10464,11685]）top-5：`'，' 2.4062`、`'！' 1.0312`、`'吗' 0.8242`、`',' 0.8008`、`'。' 0.6133`；两次运行完全一致，已存 `ref_step.npz`。CPU 单步耗时 60～116s（bf16 在 4 核 ARM 上走 BLAS 回退） |
| state 结构 | 每层 3 个：`att_shift` (2560)、`ffn_shift` (2560)、`wkv` 矩阵 (40,64,64) fp32；另有跨层传递的 `v_first`。32 层 state 合计仅约 22MB |
| **ATC 可行性** | **验证通过**：RWKV-7 的 WKV 递推状态更新、LoRA 门控（decay/gate/a/v）、GroupNorm、token shift 等算子在 Ascend 310B 上均可编译。首个 4 层 chunk 编译成功，产出 339MB `.om` |

### 11.3 过程中解决的两个问题

1. **fp16 导出被 ATC 拒绝**：`F.normalize` 把范数算成 fp32，导致 `Div` 两端 dtype 不一致（fp16 vs fp32），报 `The /att_1/Div op dtype is not same`。改为 **fp32 导出**（ATC 内部再转 fp16）后通过。
2. **多层打包成一个 ONNX 会丢权重**：4 层打包时只有最后一层的 FFN 权重进入计算图（初始化器仅 168.9M 参数，应为 326M；`blocks.0/1/2` 缺 `ffn.key.weight`/`ffn.value.weight`/`ffn.x_k`）。改为**每层一个 ONNX** 后完全正常——单层导出 81.3M 参数 / 32 个张量 / 325MB，与理论值一致。

### 11.4 收工时正在运行的后台任务

32 层逐层「导出 ONNX → ATC 编译」批处理（nohup，预计约 2 小时）：

```bash
# 进度查看
grep -c "OK$" /home/disk/models/rwkv7-2.9b/run_layers.log
tail -n 5  /home/disk/models/rwkv7-2.9b/run_layers.log
ls /home/disk/models/rwkv7-2.9b/layer*.om | wc -l
# 完成标志
cat /home/disk/models/rwkv7-2.9b/layers.done
```

收工时刻已产出 `layer0.om`、`layer1.om`，正在编译 layer 2/3，速率约 1 层 / 4 分钟。

### 11.5 明天继续的步骤

1. 导出并编译输出头：`rwkv7_export_head.py`（ln_out + 65536 维 head），再 `atc` 编译成 `head.om`。
2. 写 pyACL 编排器 `rwkv7_acl_run.py`：
   - embedding 查表（host 侧，读 `rwkv7.emb.weight`）
   - 依次执行 `layer0.om … layer31.om`，逐层传 `x / v_first / att_shift / ffn_shift / wkv`
   - 最后跑 `head.om` 得到 logits，贪心采样出 token
3. 用同一 prompt 与 `ref_step.npz` 的参考 logits 逐项比对，确认 NPU 结果与 CPU 参考一致（fp16 下允许 1e-2 量级误差）。
4. 比对通过后跑多 token 出词并测速（预期 4～7 token/s，受 51.2GB/s 带宽限制）。

### 11.6 关键文件（设备侧）

```text
/home/disk/models/rwkv7-2.9b/                 # 模型 + 全部脚本与日志
/home/disk/models/rwkv7-2.9b/ref_step.npz     # CPU 参考 logits
/home/disk/models/rwkv7-2.9b/rwkv7_export_chunk.py   # 单层/多层导出（工作区有副本）
/home/disk/models/rwkv7-2.9b/rwkv7_export_head.py    # 输出头导出（工作区有副本）
/home/disk/models/rwkv7-2.9b/rwkv7_step_ref.py       # CPU 单步参考（工作区有副本）
/home/disk/models/rwkv7-2.9b/run_layers.log          # 32 层批处理进度
/home/disk/cann80base/ascend-toolkit/                # 干净 CANN 8.0.RC1
/home/disk/miniconda3/envs/rwkv7/                    # transformers 侧环境
/home/disk/miniconda3/envs/npu22/                    # torch_npu/ATC 侧环境
```

---

## 十二、2026-09-12 里程碑：RWKV-7 2.9B 在 NPU 上跑通并生成中文文本

### 12.1 结论

**RWKV-7 G1j 2.9B 已在 Atlas 500 A2（310B）上完整跑通**：32 层逐层 `.om` + 输出头 `.om`，用 pyACL 串起来做单步递推推理，生成的文本与 CPU 参考在数值与语义上都一致。

### 12.2 数值校验（与 CPU 参考逐项对比）

CPU 参考（transformers，bf16）与 NPU 结果，prompt「你好」→ ids [10464, 11685]：

| rank | NPU token / logit | CPU token / logit |
| --- | --- | --- |
| 1 | 19137（`，`）/ 2.3516 | 19137 / 2.4062 |
| 2 | 19126（`！`）/ 0.9980 | 19126 / 1.0312 |
| 3 | 11053（`吗`）/ 0.8008 | 11053 / 0.8242 |
| 4 | 45（`,`）/ 0.7427 | 45 / 0.8008 |
| 5 | 10080（`。`）/ 0.5757 | 10080 / 0.6133 |

**top-5 token 及排序完全一致**；logits 最大绝对误差 0.344（32 层 fp16 累积误差，符合预期）。

### 12.3 实际生成与性能

```text
# 用例 1
prompt = 你好
生成   = '，我是你的AI助手。我可以帮你回答'
prefill 2 token 用时 0.44 s；生成 16 token 用时 3.35 s（4.77 token/s）

# 用例 2
prompt = Q: 什么是人工智能？A:
生成   = ' 人工智能是一种模拟人类智能的技术，它可以让计算机系统像人类一样'
prefill 13 token 用时 2.74 s；生成 32 token 用时 6.71 s（4.77 token/s）
```

性能小结：

| 指标 | 数值 |
| --- | --- |
| 模型加载（32 层 `.om` + head） | 23～36 s |
| 单 token 生成延迟 | 约 0.21 s |
| 吞吐 | **4.77 token/s** |
| 对比：同模型在设备 CPU 上单次前向 | 60～116 s（约慢 300～550 倍） |

### 12.4 资源占用与产物

```text
layer0.om … layer31.om   每层 163 MB  合计约 5.2 GB
head.om                  335 MB
device 内存              11.5 GB（编译期峰值实测可用 4.5 GB 以上）
```

### 12.5 工程要点（复用给 7.2B）

1. **必须单层导出**：多层打包成一个 ONNX 会丢失前面层的 FFN 权重（只有最后一层完整），单层导出则 81.3M 参数/层与理论值一致。
2. **用 fp32 导出**：fp16 导出时 `F.normalize` 会让 `Div` 两端 dtype 不一致（fp16 vs fp32），ATC 直接报错；fp32 导出后 ATC 内部自动转 fp16，产物反而只有 163MB/层。
3. **编译必须串行 + 低优先级**：`nice -n 19` + `TE_PARALLEL_COMPILER=1`，约 4 分钟/层，设备全程可登录；两路并行会把 4 核设备压到 SSH 失联甚至触发看门狗复位（实测发生过一次）。
4. **ATC 会改张量名**：输入 `x` → `x.1`，输出 `x` → `/Add_1:0:x`；需要做归一化匹配。layer 0 因为自身产生 `v_first`，ATC 会把这个输入做死代码消除（4 入 5 出），编排器需兼容。
5. **embedding 在 host 侧查表**，权重是 bfloat16，需先转 float32 再进 numpy（numpy 不支持 bf16）。

### 12.6 产物清单（设备）

```text
/home/disk/models/rwkv7-2.9b/layer*.om        # 32 层，约 5.2GB
/home/disk/models/rwkv7-2.9b/head.om          # 输出头
/home/disk/models/rwkv7-2.9b/rwkv7_acl_run.py # 编排器（--verify / 生成）（工作区有副本）
/home/disk/models/rwkv7-2.9b/verify_npu.log   # 数值校验日志
/home/disk/models/rwkv7-2.9b/gen_npu.log      # 用例 1
/home/disk/models/rwkv7-2.9b/gen_npu2.log     # 用例 2
```

---

## 十三、2026-09-13 性能优化（第一轮）

### 13.1 耗时归因（实测）

| 项目 | 数值 | 说明 |
| --- | --- | --- |
| 单层 `execute`（孤立） | 4.54 ms | 163MB 权重 → 36 GB/s，约为 51.2GB/s 峰值的 70% |
| 单层含搬运 | 4.92 ms | 搬运仅 0.36 ms，说明瓶颈不在 host↔device 拷贝 |
| head `execute` | 8.93 ms | 335MB 权重，单次 matmul 即受带宽限制 |
| 串联 32 层 + head（v1） | 215 ms/token | |
| 串联（v2 零拷贝） | 200 ms/token → 5.00 token/s | |
| 纯带宽下限 | 108 ms/token → 9.24 token/s | 5.5GB 权重 ÷ 51.2GB/s |

结论：**fp16 下瓶颈是权重带宽**，不是算子数量；每 token 必须完整读一遍 5.5GB 权重。

### 13.2 已实施的优化

1. **零拷贝串层（v2）**：层间 `x`/`v_first` 直接复用上一层设备输出缓冲，state 在设备上原地更新
   → 每 token 从 32 次同步往返降到 1 次 H2D + 1 次 D2H；**215 → 200 ms/token（+7%）**
   → 数值校验 top-5 仍与 CPU 参考完全一致（最大误差 0.3438，与优化前相同）
2. **常驻服务**：模型只加载一次（加载耗时 23～45s），消除每请求冷启动
3. **对话层**（`rwkv7_chat.py`）：官方 chat 模板 + 固定 system 提示词 + 停止符检测 + 重复检测 + 流式输出

### 13.3 已验证无效的路径（避免重复投入）

| 尝试 | 结果 |
| --- | --- |
| ONNX 图简化（onnx-simplifier，317→126 节点，-60%） | 4.544 → 4.589 ms，**无提速** |
| ATC 权重压缩 `--enable_compress_weight=true` | `.om` 163773894 → 163773933 B，**该特性在 310B 上不生效** |
| ATC 精度模式 | 已是 310 默认的 force_fp16，无空间 |

### 13.4 修复的 bug（由用户实测发现）

流式输出为避免提前打印停止符 `User:`，引入了 7 字符回看窗口，但**生成结束时没有把缓冲里的最后 7 个字符补打出来**，导致每个回答的结尾被吞掉。

- 修复：生成结束后按 `trim(text)` 补打剩余正文
- 新增回归测试 `test_chat_stream.py`（不依赖 NPU，用假引擎喂固定 token 序列）：换行停止符 / 无换行停止符 / 结尾不丢字 三例全部 PASS

### 13.5 当前指标

```text
模型加载     23～45 s（每进程一次）
生成吞吐     5.0～5.8 token/s（实测多次）
prefill      约 0.17 s/token（prompt 仍是逐 token 处理，是当前最大体验短板）
数值正确性   与 CPU 参考 top-5 完全一致
显存占用     32 层 .om 约 5.2GB + head 0.34GB
```

### 13.6 下一步

1. **批式 prefill**：ATC 支持 `--dynamic_dims`，计划让层图带动态 T（1 与 64 共用一套图，内存不增），
   把 85 token 提问的 14.5s 压到 1s 以内。风险点：T>1 需用模型自带 chunked WKV 实现，其中可能含 ATC 不支持的算子。
2. **层合并**（4 层/图）：把 33 次图启动降到 9 次，预计 200 → 166 ms/token（6.0 token/s）。
3. 量化（int8）受限于设备无 AMCT 工具、ATC 不支持 ONNX Q/DQ 算子，暂缓。

---

## 十四、2026-09-14 量化探索：int8 为什么在这台设备上用不起来

### 14.1 结论

**int8 不可用，最终保留 fp16。** 速度能上去（最高 2.2 倍），但输出质量撑不住——
根因是这台设备可用的量化工具链太粗，缺少 LLM 量化最关键的两项能力。

### 14.2 五套方案的实测对照

| 方案 | 生成速度 | 单层体积 | 输出质量 |
| --- | --- | --- | --- |
| **fp16（最终采用）** | **5.3 ~ 5.75 token/s** | 163 MB | 正常 |
| label-free int8（全 32 层） | 11.6 token/s（2.2×） | 86 MB | 崩坏（复读/乱码） |
| 校准 int8（全 32 层） | 7.9 token/s | 90 MB | 8 层以上退化 |
| 自研 FFN-only int8 | 6.4 token/s | 106 MB | 8 层开始退化 |
| 仅输出头 int8 | 6.2 token/s | — | 崩坏 |

层数梯度（label-free）：4 层 5.78 tok/s（正常）→ 8 层 6.42（崩）→ 16 层 7.76 → 24 层 9.83 → 32 层 11.6。

### 14.3 误差是怎么测出来的（方法）

早期用随机小数输入测出的"单层误差 0.7%"是**假象**。改用真实激活值后结论完全不同：

1. `capture_calib.py`：跑 20 步真实推理，把每层的真实输入（x / v_first / att_shift / ffn_shift / wkv）D2H 存下来；
2. `cmp_layer_real.py`：用这些真实输入逐层对比 fp16 `.om` 与量化 `.om` 的输出误差。

结果：**label-free 每层的循环状态 wkv 相对误差 20%~55%**（layer4 0.47、layer14 0.55、layer31 0.42），
残差流 x 误差 4%~110%。用真实数据校准后降到 1.7%~9.5%（layer31 的 wkv 误差 0.42 → 0.036，改善 12 倍），
但 32 层递归放大后仍然失忆。

### 14.4 根因

各层激活幅度差近百倍：x 峰值从 0.05 到 8.2，wkv 状态峰值从 1 到 146。
而工具链用的是**离线静态量化刻度**，长尾把有效分辨率吃光了（示例：某层激活峰值 6.5，
int8 的 127 级量化步长约 0.05，对典型值 0.1~1 的元素就是 5%~50% 的相对误差）。
又因为这是单 token 推理，激活张量只有一行，per-tensor 等价于 per-token，没有更细粒度可用。

工具链缺口（CANN 8.0.RC1）：没有 per-channel/per-token 激活量化、没有 SmoothQuant 类的离群值处理、
**没有仅权重量化（W8A16）**。而带宽大头正是权重（每 token 读 5.2GB）；激活只占 22MB。
换言之"只压权重、激活保持 fp16"本是对症的方案，但 ATC 没有对应开关，
`AscendWeightQuant` 虽在 310B 算子表里存在，却是量化图内部的布局搬运算子，不是权重解量化算子。

### 14.5 副产品

为验证"压哪些矩阵"而写的 `quantize_layer_sel.py`（白名单选择性量化）与
`build_{int8,calib,ffn}_layers.sh` 三套流水线保留在仓库，作为"工具链能力边界"的证据。

---

## 十五、2026-09-14 7.2B 量级的可行性测算

| 配置 | 权重占用 | 能否装下（总内存 11.5GB，系统约占 3.2GB） |
| --- | --- | --- |
| 2.9B fp16（现状） | 5.9 GB | ✅ 余量约 2.5GB，实测 5.3~5.75 token/s |
| 7.2B fp16 | 约 14.4 GB | ❌ 超过整机总内存 |
| 7.2B int8 | 约 7.2 GB | ❌ 加系统与运行时约 11.2GB，贴死上限；无 swap + 60s 硬件看门狗 → 工程不可行 |
| 7.2B int4 | 约 3.6 GB | ⚠️ 能装，但精度比已不可用的 int8 更差，且 310B 的 int4 矩阵乘未验证 |

**结论：本机合理上限是 2.9B。** 7.2B 需要 32GB 级显存/内存的设备（如 Atlas 800I A2），
或采用"边缘端文本推理 + 云端大模型"的协同架构。

补充：即便 7.2B int8 能塞下，每 token 要读 7.2GB 权重，理论吞吐约 2.5~3 token/s，
比 2.9B fp16 更慢——**容量和质量两头都不划算**。

---

## 十六、2026-09-19 从"能跑"到"能用"：服务化与网页版

### 16.1 关键提升：系统提示词状态缓存

原来每次开新对话都要把 50 个 token 的系统提示词重新跑一遍（约 10 秒）。
现在把"读完 system 提示词之后的状态"存成快照（32 层 × 3 个状态，约 22MB），
之后每轮只处理用户那一句话：

```text
首字延迟      15.8 s  →  3.1 ~ 4.8 s
缓存加载      0.2 s（命中时）
```

### 16.2 常驻 HTTP 服务 + 网页前端

- `rwkv7_http.py`：`/chat`、`/chat/stream`（SSE 流式）、`/health` + 静态托管
- `web/index.html`：单文件、无框架、无 CDN（离线可用）；气泡对话、流式打字、
  代码块渲染与一键复制、长度选择、新对话、状态行（预填充/token/速度/停止原因）、深色与移动端适配
- `test_web.py`：16 项验收（含路径穿越防护、多轮上下文、375px 无横向滚动）

### 16.3 当前指标（实测）

```text
生成吞吐     5.5 ~ 5.75 token/s
prefill      3.1 ~ 4.8 s（仅用户这一句）
首字延迟     4 ~ 7 s（流式）
端到端       128 token ≈ 23 s；192 token ≈ 35 s
模型加载     43 ~ 49 s（常驻服务只在启动时一次）
内存占用     权重 5.5GB + 运行时 ≈ 5.9GB
```

### 16.4 运维：开机自恢复与看门狗

- crontab `@reboot` → `boot_all.sh`：拉起 `slogd` / `dmp_daemon`（**这两个不自启，缺了 npu-smi 会报 -8010**），
  再启动网页服务
- crontab `*/5` → `check_service.sh`：健康检查，连续 3 次失败才重启（早期版本单次失败就重启，会误杀）
- `service.busy` 忙标记：单线程服务在推理时无法响应健康探测，用它告诉看门狗"别重启"

### 16.5 三条踩坑记录（都是真金白银）

| 现象 | 根因 | 修法 |
| --- | --- | --- |
| 服务在推理时报 `execute 失败: 107002` | pyACL 的 context **绑定创建它的线程** | HTTP 服务必须单线程（`HTTPServer` 而非 `ThreadingHTTPServer`） |
| 服务偶发长时间无响应、新请求排队 | HTTP/1.1 keep-alive 下，浏览器的**空闲长连接**把单线程服务器堵在读取上 | 改 `protocol_version = "HTTP/1.0"`，一条连接只服务一个请求 |
| 服务被看门狗反复重启 | 单次探测失败即判死 | 超时 30→45s、连续 3 次失败才重启、失败时记录进程/端口/连接数现场 |

另外：**一份模型占 5.5GB，两个实例同时驻留会把 11.5GB 的机器拖死**——
因此 `ask.sh` 改为"服务在跑时走 HTTP 客户端"，不再重复加载模型。

---

## 十七、2026-09-19 三个后处理 bug 的定位与修复

这一轮 bug 的共同点：**保护机制本身过于激进**。在小模型 + 长上下文场景下，
后处理规则要么精确、要么宁少勿多，否则修掉一个会引入另一个。

### 17.1 停止符误切正文（最严重）

**现象**：提问"写一段带 System 提示词的例子"，网页回答**空白**；问对话角色相关问题时答案被截断。

**根因**：停止符列表里含不带换行的 `User:` / `Assistant:` / `System:`，只要答案正文出现这些词
（讲提示词工程时很常见）就被当成"模型开启新一轮"而截断；极端情况在**位置 0** 命中，整段被切掉。

**定位方法**：写了 `diag_trim.py` 打印**未经截断的原始输出**。
之前的对比实验（流式 vs 非流式）看不出问题，因为两条路径都经过同一个截断函数——
**这是本次排查最重要的一条方法论**：验证要用与故障路径不同的观测面。

**修复**：只有标记位于"轮次边界"（前一字符是换行或句末标点）才截断，且位置 0 不算；
截断后去尾部空白。回归测试 `test_trim.py`（7 项，后扩到 11 项）。

### 17.2 网页结尾残留"假续写"碎片

**现象**：答案结尾出现 `UserUs` 这类碎片。
**根因**：网页是用流式片段拼出来的，而服务端是事后才截断——已推送给浏览器的碎片留在了屏幕上。
**修复**：`done` 事件带上服务端最终文本，前端以它为准重绘；回看窗口加长一个字符。

### 17.3 代码题被复读保护误切

**现象**：快速排序只写到 97 token 就被判"复读"掐断（`right = [x for x in arr if x`）。
**根因**：复读检测按 24 字符窗口查重复子串，而代码里的循环、列表推导式、重复属性名
天然带重复片段——正文用的保护规则不适用于代码。
**修复**：**代码围栏（``` 之间）内跳过复读检测**。之后快速排序、二分查找都能写完整（192 token）。

### 17.4 回答质量调优

- 系统提示词追加约束：只回答最后一个问题、不复述用户的话、不重复此前内容
- 新增**复述回退重试**：生成前对状态做快照，首答若只是在复述用户问题（或为空），
  回滚状态并加大重复惩罚重试一次
- 复读检测窗口 16 → 24 字符，且要求窗口内至少 12 个非空白字符

---

## 十八、2026-09-20 当前状态与后续

### 18.1 交付物

| 类别 | 内容 |
| --- | --- |
| 模型部署 | RWKV-7 G1 2.9B（32 层 + 输出头）编译为 `.om`，pyACL 零拷贝推理 |
| 服务 | 常驻 HTTP 服务（`/chat`、`/chat/stream`、`/health`）+ 网页前端 + 命令行入口 |
| 运维 | 开机自恢复（cron `@reboot`）+ 健康看门狗（`*/5`） |
| 实验 | 三套量化流水线 + 误差测量工具 + 全部原始日志 |
| 文档 | 本报告 + `README.md`（仓库首页）+ `PROGRESS.md`（逐日记录） |
| 代码库 | https://github.com/Erformance/atlas500a2-rwkv-course-assistant（私有） |

### 18.2 可回归的验证

```bash
python test_web.py     # 网页与接口：16 项，全部通过
python test_trim.py    # 停止符与复述判定：11 项，全部通过（设备上跑）
```

### 18.3 后续可做（按性价比排序）

1. **批式 prefill**：把 85 token 提问的 15.8s 压到 1~2s。当前受阻——
   模型的 chunked WKV 实现用了 `aten::linalg_solve_triangular`，ONNX 导不出，
   需要改写该段数学（三角方程组前向代入）后才能导出 T>1 的图。
2. **多会话隔离**：引擎已支持状态快照导入导出（每份 22MB，切换约 0.1 秒），
   可支持多个学生同时使用而不串上下文。
3. **访问口令**：给网页加一层简单校验；目前任何能访问局域网的人都能用。
4. **图文问答**：图片交给外部视觉 API 转成文字描述后拼进 prompt，本机侧无需改动——
   这台设备的内存（11.5GB，已被文本模型占去一半）不适合再塞视觉编码器。
