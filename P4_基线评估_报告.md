# P4 基线评估报告（协议 §6）

日期：2026-09-26 ｜ 设备：Atlas 500 A2（昇腾 310B，11.5GB 共享内存，无 swap）

## 1. 协议要求与本轮范围

协议 §6 要求：基线至少覆盖"参考浮点 OM、修复校准后的静态 INT8、固定 FFN 规则、固定敏感路径保护、
局部误差驱动混合精度、提出的完整方法"；**优先与 RWKVQuant 的语言评估任务对齐**
（LAMBADA、HellaSwag、PIQA、ARC-Easy/Challenge），使用**固定版本的 lm-evaluation-harness**，
自定义 backend 完成同 tokenizer 的 loglikelihood/generation 接口，先核对 CPU 与 OM 的少量逐题结果。

本轮（P4-1）做到的部分：

| 要求 | 状态 |
| --- | --- |
| 固定版本 lm-evaluation-harness | ✅ `lm-eval==0.4.13`（隔离 venv，见 §2） |
| 自定义 backend（同 tokenizer 的 loglikelihood/generation） | ✅ `p4_score_server.py` + `p4_lm_eval_backend.py` |
| 先核对少量逐题结果 | ⚠️ 已完成"口径方向检查"（§3）；OM↔PyTorch CPU 的逐题对照见 §6 未做项 |
| 在真实任务上对比精度 | 🔄 PIQA + LAMBADA 子集，fp16 与均衡档（§4） |
| RWKVQuant 复现 | ❌ 未做（见 §6） |
| 第二个更小 RWKV-7 checkpoint | ❌ 未做（见 §6） |
| 中文课程题盲评 | ❌ 未做（见 §6） |

## 2. 基础设施

| 组件 | 说明 |
| --- | --- |
| 打分服务 | `p4_score_server.py`（npu22 环境）：加载**一次**引擎（与网页服务同一套 `rwkv7_serve2.Engine`、同一份 `.om`、同一个 `tokenizer.json`），HTTP 暴露 `POST /loglikelihood`、`POST /loglikelihood_rolling`、`POST /generate_until`、`GET /health` |
| harness backend | `p4_lm_eval_backend.py`：注册 `rwkv7_npu`，把 harness 的请求批量转发给打分服务 |
| 运行入口 | `p4_run_eval.py`（调用 `lm_eval.simple_evaluate`，无 bootstrap、保留样本级结果） |
| Python 环境 | `/home/disk/lmeval`（venv，`--system-site-packages` 复用 npu22 的 torch 2.2.0 与 tokenizers 0.23.2，避免污染推理环境）；lm-eval 0.4.13、datasets 5.0.1、evaluate 0.4.6 |
| 数据缓存 | `HF_HOME=/home/disk/hf_cache`、`HF_ENDPOINT=https://hf-mirror.com`（设备根分区只有 5.9G，缓存必须落在大盘；本轮也因此清了 1.17G pip 缓存、把 `/root/rwkv_project` 挪到 `/home/disk/old_root_rwkv_project`） |
| 任务数据 | 由 harness 从 Hub 拉取并缓存；每次运行的 `configs` 里带数据集版本，输出 JSON 里一并保存 |

## 3. 冒烟与口径检查（已完成）

1. **打分方向**（`p4_smoke_test.py`，fp16 档）：

   | 前缀 | 续写 | sum logp | is_greedy |
   | --- | --- | --- | --- |
   | Question: How do you close a door? Answer: | " Turn the handle and push." | −9.263 | False |
   | 同上 | " boil water in a pot" | −23.951 | False |
   | The capital of France is | " Paris" | −0.259 | **True** |
   | The capital of France is | " Tokyo" | −9.888 | False |

   正确续写显著高于错误续写（−9.3 vs −24.0、−0.26 vs −9.9），且 "Paris" 被判定为贪心续写 ✓。
2. **生成接口**：`generate_until("The capital of France is", max 8, until "\n")` → `" Paris."` ✓
3. **harness 端到端**：`piqa` 子集 5 题 → `acc = 0.80`，用时 106 s（含数据集下载与 harness 开销）。

## 4. 试点结果（PIQA + LAMBADA 子集）

口径：`--limit 200`（每任务前 200 题，同一子集）、贪心解码、two tasks 合并到一次 harness 运行；
精确档 = 全部 fp16，均衡档 = p1 top8（8 层 int8 + 25 层 fp16，线上默认档）。

| 任务 / 指标 | 精确档 fp16 | 均衡档 p1 top8 | 差值 |
| --- | --- | --- | --- |
| PIQA `acc` | **0.790** | **0.780** | −1.0 pp（200 题里差 2 题） |
| PIQA `acc_norm` | 0.795 | 0.790 | −0.5 pp |
| LAMBADA (openai) `acc` | **0.715** | **0.700** | −1.5 pp（差 3 题） |
| LAMBADA (openai) `perplexity` | **3.7745** | **4.2484** | **+12.5%** |
| 端到端耗时 | 6291 s | 5086 s | **快 1.24×** |

**判读**：

1. **准确率基本无损**：PIQA −1.0 pp、LAMBADA −1.5 pp（各差 2~3 题），在 200 题子集的噪声范围内；
   与 P3 测到的"均衡档全序列 KL 0.0141"一致——8 层 int8 对答案选择几乎没影响。
2. **困惑度是有代价的**：LAMBADA perplexity 从 3.77 升到 4.25（+12.5%）。这说明
   **准确率类指标对这类量化不敏感，而概率分布类指标敏感**——与 P2-C/P2-F 的结论同向
   （偏差存在但很少改变最终选择）。
3. **速度收益是实打实的**：同一子集 5086 s vs 6291 s（1.24×），与 P5 的 decode 速率比
   （6.02 / 5.07 = 1.19×）一致，说明子集口径下的性能结论可以外推。
4. 数值量级自检：2.9B 模型在本机 fp16 下拿到 PIQA 79.0% / LAMBADA 71.5%，
   属于该规模模型的合理区间，说明"逐层 ONNX + ATC + pyACL 零拷贝"这条路没有把模型跑歪。

**口径限制**（必须在引用时一起说明）：① 只是每任务前 200 题的固定子集，不是官方全量；
② 没有 bootstrap 置信区间（子集太小，`bootstrap_iters=0`），差值应按"差 2~3 题"理解；
③ 未做 CPU 与 OM 的**逐题**对照（见 §6 第 1 条）。

## 5. 为什么只能做子集：完整基准的成本测算

实测（fp16 档、子集 5 题）：PIQA 平均约 **15 s/题**（每题两个候选续写，各要预填充前缀+续写）。
结合 P5 的固定工作量数据（fp16 prefill = decode = 5.07 tok/s），折算完整基准的**本机成本**：

| 任务 | 官方题量 | 单题耗时（估计） | 单档位全量成本 |
| --- | --- | --- | --- |
| PIQA | 1838 | 15 s | ≈ 7.7 h |
| LAMBADA（openai） | 5153 | 8 s | ≈ 11.4 h |
| ARC-Easy | 2376 | 12 s | ≈ 7.9 h |
| HellaSwag | 10042 | 20 s | ≈ 55.8 h |
| **四任务合计** | — | — | **≈ 83 h / 档位** |

也就是说，按协议理想的"四任务 × 多档位全量"在本机需要数周，**不可行**。因此本轮采用
**固定子集 + 明确标注**的做法：所有子集题号与随机种子固定、结果 JSON 保留样本级明细，
并且报告里同时给出"折算全量成本"，便于他人判断可比性。

## 6. 未做与下一步

1. **OM ↔ PyTorch CPU 的逐题对照**：协议要求"先核对 CPU 与 OM 的少量逐题结果"。
   本项目的 P0-D 已经做过 NPU vs CPU 的逐步 logits 对照（top-5 一致、最大误差 0.3438），
   但**没有**在 lm-eval 的题目粒度上做；下一步取 2~3 道 PIQA 题，用设备上的 PyTorch 参考
   （CPU，bf16）复算 logp 并与打分服务对齐。
2. **RWKVQuant 复现**：需要其量化方案与 checkpoint；本机没有对应的 VQ kernel，
   按协议应标注"该设备未实现"，只能做**准确率—存储对照**，不能把它的数字混进 NPU 速度表。
3. **第二个更小的 RWKV-7 checkpoint**：需要新下载并重走"逐层导出 ONNX → ATC 编译 → 校准"
   全流程（按 P1 的经验，32 层编译约 4 h + 校准采集 0.5 h），属于独立工作量。
4. **中文课程题盲评**：协议要求 100~200 道未参与校准的中文课程题 + 人工评分标准。
   本项目已有 64 段分离语料（`p1_corpus.py`），但那是"校准/验证/测试"文本，不是带标准答案的题目集。
5. **ARC-Easy / ARC-Challenge / HellaSwag**：受 §5 的算力限制，建议以子集形式补测。
