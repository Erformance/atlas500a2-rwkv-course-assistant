# P0 报告：校准采样时序与参考一致性（A/B/C/D 四项）

日期：2026-09-22　对应协议：`02_补实验执行协议.md` 第 2 节
设备：Atlas 500 A2（昇腾 310B，EulerOS 2.0 SP11）；`manifest.json` 已冻结

## 0. 总判定

| 项 | 内容 | 结果 |
| --- | --- | --- |
| P0-A | 首 token 零状态与采样时序 | **发现问题并定位**：旧校准采样取的是执行后状态；x/v_first 差 0，三个状态差 30%~142% |
| P0-B | 同块重放 | **通过**：重放与在线执行逐位一致（layer 0/1/15/31） |
| P0-C | 原地 vs 双缓冲 | **通过**：1/8/128 步全部逐位一致 |
| P0-D | 参考链路 | **通过**：NPU top-5 与 CPU 参考一致，最大 logits 误差 0.3438 |

按协议"P0 通过标准"（采样可重放、首步状态正确、别名行为可解释、参考链路误差稳定且被记录）：
**P0 通过**，且必须先修正校准数据再讨论量化结论。

## 1. P0-A：采样时序

### 1.1 代码证据

`rwkv7_serve2.py` 每层的输入/输出绑定：

```python
slot = {"x": src_x, "v_first": src_vf,
        "att_shift_0": self.att[i], "ffn_shift_0": self.ffn[i], "wkv_0": self.wkv[i]}
dst  = {"x": self.layer_x[i], "v_first": self.layer_vf[i],
        "att_shift_out_0": self.att[i],      # ← 与输入同一块内存
        "ffn_shift_out_0": self.ffn[i],      # ← 同一块
        "wkv_out_0": self.wkv[i]}            # ← 同一块
```

旧脚本 `capture_calib.py` 在 `engine.step()` 之后采样，因此读到的是**更新后的状态**。

### 1.2 实测差异（16 步小试，276 token 前缀）

相对量级 = 新旧时序平均绝对差 ÷ 真实输入均值绝对值。

| 层 | 张量 | 平均绝对差 | 相对量级 |
| --- | --- | --- | --- |
| layer0 | x | 0.000000 | **0.0000** |
| layer0 | att_shift_0 | 0.019625 | 1.3053 |
| layer0 | ffn_shift_0 | 0.182520 | 1.3046 |
| layer0 | wkv_0 | 0.000311 | 0.2969 |
| layer1 | att_shift_0 | 0.448282 | 1.3881 |
| layer1 | ffn_shift_0 | 0.209081 | 1.3574 |
| layer1 | wkv_0 | 0.011939 | 0.4671 |
| layer31 | att_shift_0 | 0.784162 | 0.8830 |
| layer31 | ffn_shift_0 | 1.339704 | 0.9519 |
| layer31 | wkv_0 | 0.057877 | 0.6597 |

`x` 与 `v_first` 全 32 层差异**精确为 0**（它们的输入由上一层产出、本步不再被覆盖），
差异恰好只出现在原地更新的三个状态上——这是问题定位的直接签名。

## 2. P0-B：同块重放（通过）

做法：在线跑 8 步，在最后一步捕获每层的完整输入（执行前）与输出（执行后）；
为被测层**新建独立 OM 实例 + 独立输入/输出缓冲**，喂入捕获的输入，与在线输出比对。

结果：layer 0 / 1 / 15 / 31 的 5 个输出张量全部 **最大绝对差 0.000e+00，逐位一致**。

意义：① 输入/输出名称映射与缓冲语义可复现，不依赖未检查的列表顺序；
② 证明"执行前采样"拿到的确实是该层消费的输入——**新校准数据的方法学可信**。

## 3. P0-C：原地 vs 双缓冲（通过）

做法：同一权重、同一 x/v_first（固定）、同一初始状态（全 0），只改变状态缓冲用法：
原地（状态输出写回输入缓冲）与双缓冲（写到独立缓冲再 D2D 搬回），比较逐步快照与最终状态。

| 步数 | 最终状态最大差 | 逐位一致 | 逐步一致 |
| --- | --- | --- | --- |
| 1 | 0.000e+00 × 3 | 是 | 是 |
| 8 | 0.000e+00 × 3 | 是 | 是 |
| 128 | 0.000e+00 × 3 | 是 | 是 |

意义：引擎的零拷贝原地更新是**安全契约**，不是数值风险的来源；后续性能数字中的原地复用无需额外复制。

## 4. P0-D：参考链路（通过）

固定 token 序列，比较 PyTorch/CPU 参考与 fp16 OM：

```text
NPU 跑 2 token 用时 0.415 s（207.7 ms/token）
NPU top5: [(19137, 2.3516), (19126, 0.998), (11053, 0.8008), (45, 0.7427), (10080, 0.5757)]
CPU top5: [(19137, 2.4062), (19126, 1.0312), (11053, 0.8242), (45, 0.8008), (10080, 0.6133)]
top-5 一致: True ；最大误差: 0.3438
```

误差量级与 fp16 舍入一致，链路（PyTorch → ONNX → fp16 OM）无系统性偏差。

## 5. 据 P0 结论修正后的量化小试（P2-A 的前半）

用**正确时序**数据重做 layer0 / layer31，与 label-free、旧校准对比
（误差用 `calib_pre` 的真实输入测量，最大相对误差）：

| 层 | label-free | 旧校准（错误时序） | 新校准（正确时序） |
| --- | --- | --- | --- |
| layer0 | 0.3008 | **0.9442** | **0.2666** |
| layer31 | 1.1132 | —（未生成该层产物） | **0.4496** |

**要点**：旧校准因时序错误，在 layer0 上甚至**比 label-free 更差 3 倍**；
换成正确时序后 layer31 误差从 1.113 降到 0.450（**2.5 倍改善**）。
但残余误差仍有量级，说明工具链的离线静态 per-tensor 激活量化本身较粗——
这与之前"缺 W8A16/分通道激活量化"的判断一致，两者是叠加的，不是二选一。

## 6. 工程性发现（协议外，但影响实验可复现性）

1. **实验必须独占 NPU**：看门狗每 5 分钟会拉起网页服务，与实验抢设备
   （表现为 `.om` 加载失败 145001）。已在 `check_service.sh` 增加 `experiment.lock`
   机制：锁存在时看门狗全程不介入。
2. 计时与采样脚本若直接用 `OmModel`，必须自行 `acl.init()/set_device/create_context`，
   否则加载失败——本报告中的脚本已修正。

## 7. 交付物

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| manifest | 设备 `manifest.json` | 权重/tokenizer 哈希、硬件与软件版本、编译命令、数据 ID |
| 正确时序校准数据 | 设备 `calib_pre/layerNN.npz`、`head.npz` | 32 层 × 16 步（状态年龄 0~15） |
| A/B 差异统计 | 设备 `p0_sampling_ab.json` | 第 1.2 节数据的逐层完整版 |
| 重放/等价性证据 | 设备 `p0b_replay.json`、`p0c_alias.json` | P0-B / P0-C |
| 脚本 | `capture_calib_pre.py`、`p0b_replay.py`、`p0c_alias.py`、`build_cp_layers.sh` | 已入仓库 |

## 8. 下一步（进行中）

**全栈重校准**：`build_cp_layers.sh` 已于 21:37 启动，用正确时序数据重建 32 层 + 输出头
（`layer{i}_cp_q.om` / `head_cp_q.om`，预计约 3.5 小时）。完成后按协议 P2-A 做端到端 A/B：
同一批提示词，比较 fp16、旧校准 INT8、新校准 INT8 的连续轨迹指标与生成质量，
再决定是否扩展到 P3 的时域指标研究。
