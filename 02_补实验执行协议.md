# 补实验执行协议：交给工程负责人

版本：2026-09-20。配套阅读 `01_研究方案与理论框架.md`。这里的窗口、次数和数据量是建议起点，不是已经完成的实验，也不是会议录用标准。先执行 P0–P2，通过后再扩大矩阵。

## 0. 一句话任务

先把校准采样和基线评估做对，再验证“面向后续输出的时域指标”是否在相同资源预算下优于局部误差，最后才做完整 NPU 性能和精度搜索。

## 1. 开工前的交付物

保存仓库 commit、未提交差异、权重/词表哈希、模型结构配置、ONNX/OM 哈希、ATC 命令、量化配置和校准样本 ID。记录 Atlas 型号、芯片、可用共享内存、系统、驱动、固件、CANN、NNRT、Python/ACL 和量化工具版本。模型名称必须写完整，base 与 instruct 不能混作同一基线。

输出 `manifest.json`，至少包含 model_revision、tokenizer_hash、git_commit、hardware、software_versions、compile_commands、candidate_plan、calibration_ids、validation_ids、test_ids、seeds、generation_settings。

每个候选 OM 记录实际匹配和量化的节点、权重/激活/累加/状态/输入输出 dtype、scale/zero-point、编译警告、workspace 和 fallback。文件不存在、节点白名单为空或加载计划不符应立即报错，不悄悄使用浮点替代。无许可证或来源说明的复用代码先向作者确认授权；不把第三方实现作为自己的原创。

## 2. P0：校准时序与参考一致性

### P0-A 首 token 零状态测试

对正常浮点引擎 reset，输入一个 token。在**每个块执行前**读取输入，在该块执行后读取输出。检查 att-shift、ffn-shift、WKV 输入是 reset 后的真实初值，输出是更新值。将当前脚本的执行后“输入采样”与新采样比较。

交付：两种采样的 buffer 名称、dtype、shape、时间点和摘要，以及一次能复现差异的最小程序。用哈希和统计摘要避免在日志里打印完整用户文本或数百万状态元素。

### P0-B 同块重放测试

把新采到的完整输入送入同一个浮点 OM，从独立输出缓冲区读取结果，与在线执行对应输出比较。固定输入/输出名称映射，不依赖不受检查的列表顺序。要求误差与该实现本身重复执行的数值基线一致；不要未经核验就设跨平台“绝对零误差”要求。

### P0-C 原地与双缓冲测试

同一权重、token、初始状态，比较输入输出别名与 ping-pong 状态缓冲两种执行，覆盖 1、8、128 个步。当前 OM 是否支持原地执行必须用实际契约和数值测试确认。新增复制只用于诊断，不混进最终优化性能数字。

### P0-D 参考链路测试

检查 PyTorch reference → ONNX reference → floating OM。固定 token 序列，逐步比较关键块和 logits。参考 dtype、状态舍入位置、归一化、词表与导出设置保持明确。先发现导出或算子问题，再研究量化。

**P0 通过标准：** 采样可重放；首步状态正确；别名行为可解释；参考链路误差稳定且被记录。否则暂停所有“量化导致失稳”的结论。

## 3. P1：校准数据与存储设计

校准、选计划的 validation、最终 test 分离；数据 ID 和分割哈希进入 manifest。不要用测试题反复调 scale、阈值或提示词。AI、牛顿等仓库演示提示不能承担最终质量评估。

建议先用独立的 16–32 段短文做小试，再扩至 64–128 段；覆盖中文、英文、问答、叙述及少量代码，比例按目标任务预先规定。每段先输入真实前缀使状态进入非零轨迹，然后只在分层采样的位置保存输入。可试状态年龄 0、64、256、1024，资源不够时先完成 0、64、256。校准覆盖不足和状态时序错误是两个不同因素，实验应分别控制。

**不能把所有层、所有 token 状态无差别存盘。** 按当前代码常量计算，32 块 FP32 WKV 加两类 shift state 为

`32 × (40 × 64 × 64 + 2 × 2560) × 4 = 21,626,880 bytes`

这是约 21.63 MB/全状态快照，未包含其他张量、压缩和存储开销。128 段、每段 1024 步都保存全状态会达到约 2.83 TB 原始量级。因此应采用每层 reservoir、少量完整快照、流式统计和限定窗口；不要在 11.5 GB 级共享内存设备上长期囤积列表。这个容量是由配置推导的估计，不是实测磁盘用量。

Gramian 指标还需要参考轨迹中的每 head 的 decay、归一化方向、gate 和 readout。现有普通块边界快照不自动提供这些中间量；需在 PyTorch 参考中加 hook，或导出单独诊断图。诊断图可能改变融合，不用于最终性能计时。稠密探索可在 CPU/GPU 离线进行，关键候选再以目标 OM 校核；模拟不能代替 NPU 实测。

## 4. P2：错误来源分解实验

| 组别 | 改变的因素 | 保持不变 | 要回答的问题 |
|---|---|---|---|
| A | 旧采样 vs 执行前采样 | 相同文本、采样数量、量化设置 | 时间戳错误影响多少？ |
| B | 开头采样 vs 分层状态年龄采样 | 正确时序、样本数量和量化设置 | 校准覆盖是否是主要因素？ |
| C | 每步注入参考状态 vs 各自保留状态 | 同一强制 token 序列 | 多步状态传播是否造成额外偏差？ |
| D | 单次量化脉冲 vs 持续量化 | 同一前缀、同一候选 | 单次传播代理能否预测持续注入？ |
| E | head-only、FFN-only、recurrent-only | 同一数据、精度预算和停止设置 | 问题位于哪条路径？ |
| F | 强制 token vs 自由生成 | 同一初始前缀与精度计划 | 生成反馈放大多少偏差？ |

特别负对照：只量化最终输出头、并强制给定相同 token 时，内部 recurrent state 不应仅因 head 量化而改变；自由生成时则可能通过不同 token 间接改变。这项测试有助于区分状态传播与输出决策反馈。

每段记录 logits KL、top-1 一致率、参考 top-1/top-2 margin、归一化状态差、输出差、量化 clipping 比例。局部指标和连续指标分别命名，不能把现有 mean-absolute-error 比值标成相对 L2。

## 5. P3：理论指标是否真的有价值

对可编译候选在 H=1、8、32、128 下估计指标，H 为读出次数，含注入当步。先做小模型；2.9B 上只保留经过筛选的实验点。计算预算和编译数量必须记账，不能让提出的方法靠更多搜索获胜。

比较以下敏感度：权重 MSE；同输入单步输出 MSE；归一化状态 Frobenius 误差；局部 logits KL；输出加权 Gramian；完整短 rollout KL。最后两项可以组成两阶段方法，也要单独消融。

在未用于选择指标的 validation/test windows 上检查 Spearman 排序相关性、候选保留率、top-k 召回率及最终计划的质量—延迟结果。按文档/请求做配对 bootstrap，保留原始样本级指标，避免把同段相邻 token 当成独立样本夸大置信度。

额外测量：稠密 Gramian 与结构化递推的数值一致性和离线耗时，报告模型大小、窗口数、head 数、H 和所用计算设备。NumPy 的 O(d²) 代码通过并不表示在 NPU 上一定更快。

**P3 通过标准：** 多步/方向指标具有可重复的增量价值，并能在匹配搜索预算下改善计划，而不是只有个别散点相关性好看。

## 6. P4：强基线与最终质量评估

基线应至少覆盖：参考浮点 OM、修复校准后的静态 INT8、固定 FFN 规则、固定敏感路径保护、局部误差驱动混合精度、提出的完整方法。固定规则需同时给相同低精度参数比例和相近真实延迟的对照，避免有利选择预算。

RWKVQuant 是直接相关强基线。优先在相同 checkpoint 上复现其质量结果；只有存在目标硬件真实执行实现，才能把它列入 NPU 速度表。没有相应 VQ kernel 时标明“该设备未实现”，不能用 CPU 回退或另一块 GPU 的数字拼接成速度比较。可另列准确率—存储对照，清楚区分精度格式和目标。[E1]

Quamba2 属于 Mamba 家族，不能无修改地作为 RWKV 的同模型实测基线。可以复现其适用结构的思想或增加 Mamba 扩展实验，但要标明“适配基线”，不能把适配结果冒称官方原实现。[E2]

模型范围：当前 2.9B，加至少一个有公开可用 checkpoint、许可证清晰的较小 RWKV-7 模型。资源允许增加第三个规模。没有第二硬件时，论文只主张该类目标 NPU 上的结果，不声称硬件普适。

优先与 RWKVQuant 的语言评估任务对齐：LAMBADA、HellaSwag、PIQA、ARC-Easy/Challenge 等。使用固定版本的 lm-evaluation-harness，自定义 backend 完成同 tokenizer 的 loglikelihood/generation 接口，先核对 CPU 与 OM 的少量逐题结果。不得只实现 generate 后把选择题回答字符串匹配当成同一评估协议。[E1, E3]

中文课程案例可另建 100–200 道未参与校准的题，由人工制定评分标准并盲评。它只能支持应用可用性，不能凭答题分数声称真实学习效果改善；后者需要独立教育研究设计。汇总均分之外保留每个任务的分数和置信区间，避免总体平均掩盖某个任务崩溃。

## 7. P5：硬件性能协议

**固定工作量微基准。** 建议前缀 32、128、512、1024、2048 tokens，decode 128 或 256 tokens，batch=1；资源不足先测 128/512 前缀和 128 decode。用完全相同的预定 token 驱动各方法，关闭内容触发的提前停止。它测的是执行性能，不是生成质量。

**真实请求延迟。** 单独记录使用相同用户提示的真实生成，报告输出长度、停止原因和采样配置。固定工作量与真实请求结果不能混在一列。

**计时范围。** cold load、prefill、TTFT、decode、采样、设备间搬运、HTTP 传输、前缀缓存命中分别列出。decode 吞吐为 `decoded_tokens / pure_decode_seconds`，不含加载和 prefill。前缀缓存打开/关闭分别报告，并写明命中多少 tokens。

充分预热后至少进行 5 次独立性能重复；测试方法顺序随机化，记录温度和电源策略。请求 P95 需要足够多独立请求支持，不能用 5 次运行就声称可靠尾延迟；样本不足时只报告原始样本和中位数。ACL 执行若存在异步路径，要在规定的同步边界计时。

内存区分权重、状态、workspace、CPU embedding、运行时额外占用和系统共享内存峰值。不能把同一共享内存当作 CPU 和 NPU 两份相加。能量只在有可靠测量接口或功率仪时报告，并说明板级/芯片级口径；没有可靠仪器就不填能耗数字。

## 8. P6：必要消融与图表

消融至少包括：H=1；G=I；移除离线指标仅保留 rollout；移除 rollout 仅用离线指标；移除设备成本；统一保护敏感路径替代学习分配；正确校准后不做新方法。旧采样只能作为诊断，不能充当唯一弱基线。

主文建议六幅图：质量—实测延迟 Pareto 曲线；按时间/块划分的误差轨迹；候选指标与测试损失关系；性能和内存分解；关键消融；搜索成本与校准预算。图中不得提前填虚构数值或仅展示最佳一次。

## 9. 输出数据格式

建议统一为 JSONL/CSV（文件中仅保留原始结果，不手动替换）：

`run_id, commit, model_hash, om_hash, plan_id, task, sample_id, seed, prefix_tokens, decode_tokens, cache_mode, latency_ms, prefill_ms, ttft_ms, peak_memory_bytes, kl, nll, task_score, stop_reason, status`

数值缺失用 null/NA，并附原因。NaN、OOM、编译失败、设备异常和质量崩坏都应保留，不能从平均结果里静默删除。完整结果与绘图脚本一同交付，原始日志只追加，不覆盖。

## 10. 最小启动包

最先返回四份材料：冻结后的 manifest；P0 校准重放报告；正确/旧采样在同一小试数据上的 A/B 结果；浮点、修复 INT8、FFN-only 三条基线在固定工作量下的速度和连续轨迹指标。

收到这些数据后即可决定主线是否成立。只有 P3 给出有效信号，再扩展完整候选搜索和大规模质量基准，避免一开始就投入昂贵的全矩阵扫描。

## 来源

[E1] RWKVQuant，ICML 2025；实验、模型和任务见 https://arxiv.org/html/2505.03803v1 。正式出版：https://proceedings.mlr.press/v267/xu25at.html

[E2] Quamba2，ICML 2025。https://proceedings.mlr.press/v267/chiang25a.html

[E3] EleutherAI, lm-evaluation-harness 官方仓库。https://github.com/EleutherAI/lm-evaluation-harness 。必须冻结实际运行版本，本文不默认最新版本。

代码观察来源与研究边界见主文 [R1]–[R5]、[R13]。
