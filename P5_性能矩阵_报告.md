# P5 性能矩阵报告（协议 §7）

日期：2026-09-25 ｜ 设备：Atlas 500 A2（昇腾 310B，4 核 ARM64，11.5GB 共享内存，无 swap）

## 1. 口径（协议 §7 要求逐项落实）

| 要求 | 本次做法 |
| --- | --- |
| 固定工作量微基准 | 前缀 32/128/512/1024/2048 token、decode 128 token、batch=1；**预定 token 驱动**，关闭内容触发的提前停止（不走 chat 的停止符逻辑） |
| 计时范围分开 | cold load / prefill / TTFT / decode 分别计时；HTTP 传输与采样开销在真实请求那一节单独报 |
| decode 吞吐口径 | `decoded_tokens / 纯解码秒数`，不含加载与 prefill |
| 充分预热 + 重复 | 每个 (臂, 重复) 独立进程建一次引擎（预热包含在内）；5 次独立重复；每次重复内**随机化前缀顺序** |
| 长前缀样本不足 | 2048 前缀单次 prefill 在 fp16 上就要 6.7 分钟 → 只跑 3 次，表中标出样本数（协议允许样本不足时只报原始样本与中位数） |
| 温度与电源策略 | 每个 repeat 前后取 `npu-smi` 温度（观测值 41~46°C）；未做调频/功耗策略干预（默认策略） |
| 内存 | 权重（磁盘 .om 体积）、状态（由常量算）、CPU embedding（由常量算）、进程 RSS、系统可用内存差值、共享内存差值**分别列出，不互相相加** |
| 能耗 | **留空**：本机没有可靠功率计或板级功耗接口，按协议不填 |

## 2. 固定工作量矩阵（中位数）

| 臂（int8 层数） | 冷加载 | 进程 RSS | 可用内存下降 | prefix 32 | prefix 128 | prefix 512 | prefix 1024 | prefix 2048 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 精确档 fp16（0） | 43.5 s | 3993 MB | 3753 MB | 6.31 s | 25.24 s | 100.9 s | 201.9 s | **398.9 s** |
| 均衡档 p1 top8（8） | 29.5 s | 3070 MB | 3168 MB | 5.31 s | 21.26 s | 84.9 s | 169.9 s | **345.1 s** |
| 快速档 p1 top16（16） | 16.4 s | 2367 MB | 2579 MB | 4.35 s | 17.40 s | 69.6 s | 139.2 s | **287.0 s** |
| 全量 int8（33，实验） | 12.9 s | 1666 MB | 1250 MB | 2.66 s | 10.65 s | 42.6 s | 85.3 s | 未取回（见 §5） |

（表中 prefill 单位为秒；TTFT ≈ prefill + 首 token 一步，因为本机 prefill 与 decode 速率相同，见 §4）

### 2.1 decode（纯解码，128 token）

| 臂 | tok/s | ms/token |
| --- | --- | --- |
| 精确档 fp16 | 5.07 | 197.3 |
| 均衡档 p1 top8 | 6.02 | 166.1 |
| 快速档 p1 top16 | 7.35 | 136.0 |
| 全量 int8 | **12.00** | **83.4** |

各臂在 5 个前缀下的 decode 速率完全一致（例如 fp16 全部 5.07、全量 int8 全部 11.98~12.00），
说明**前缀长度不影响稳态解码速率**——这是无 KV 扩容、状态定长的 RWKV 特征。

## 3. 真实请求延迟（走 HTTP 服务，均衡档，5 次请求）

真实用法下的口径与微基准不同：这里启用了内容触发的停止符，输出长度由模型自己决定
（本次 5 次都撞到 64 token 上限而未命中停止符）。

| # | 问题 | 首字延迟 | 总时长 | 输出 | 服务端 prefill | 服务端 tok/s | 停止原因 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 什么是梯度下降？ | 6.3 s | 15.3 s | 64 token | 3.7 s | 5.59 | max_tokens |
| 2 | 解释一下牛顿第一定律。 | 6.8 s | 15.7 s | 64 token | 4.3 s | 5.60 | max_tokens |
| 3 | 什么是熵？ | 5.7 s | 14.7 s | 64 token | 3.2 s | 5.59 | max_tokens |
| 4 | 光合作用分哪两个阶段？ | 6.8 s | 15.7 s | 64 token | 4.3 s | 5.60 | max_tokens |
| 5 | 写一个判断素数的 Python 函数。 | 6.4 s | 16.1 s | 64 token | 4.6 s | 5.59 | max_tokens |

热请求中位数：**首字 6.8 s、总 15.7 s**（每问都先 `/reset`，即每问都从"系统状态缓存命中 + 只 prefll 用户问题"起步）。

**与前缀缓存的关系**：服务启动时会把 system 提示词的状态缓存下来（约 276 token），此后每轮只预填充用户那一句。
没有这个缓存时，首字延迟会是"276 token 的预填充"（按 6 tok/s 约 46 s）；有缓存后降到 3~5 s 的服务端 prefill，
这正是网页端首字 6~7 s 的主要来源（其余是 HTTP 与采样开销）。

**HTTP 与服务端的差距**：同一档位，纯解码 6.02 tok/s（微基准）→ 真实请求 5.59 tok/s。
差值来自每 token 的 Python 侧开销（对 65536 维 logits 做重复惩罚、tokenizer 解码、SSE 写出）。

## 4. 三条直接可用的工程结论

1. **本机 prefill 与 decode 速率相同**（fp16 都是 5.07 tok/s，全量 int8 约 12 tok/s），
   因为每一步都是"跑完 32 层 + 输出头"的串行计算。所以 **TTFT ≈ 前缀长度 × 单 token 时间**，
   前缀越长越吃亏：1024 前缀在 fp16 上首字要 202 s。这解释了系统提示词状态缓存的必要性
   （省掉 276 token ≈ 46 s），也说明课堂场景应当把长资料放在"检索后拼接"而不是"塞进前缀"。
2. **int8 档不只是更快，还更省**：加载 43.5→12.9 s（3.4×）、RSS 3993→1666 MB（2.4×）、
   可用内存下降 3753→1250 MB（3.0×）。原因是 int8 层文件 86MB/层 vs fp16 163MB/层。
   在 11.5GB 的板上，这直接决定了"能不能同时常驻别的服务"。
3. **速度与精度可以按需交换**：精确 5.07 / 均衡 6.02 / 快速 7.35 / 全量 12.00 tok/s，
   对应 KL 0 / 0.0141 / 0.0314 / 0.6857（见 P1/P3 报告）。均衡档用 8 层 int8 换到
   1.19× 速度而 KL 只有 0.0141，这是当前性价比最优点。

## 5. 2048 前缀补充测量

第一轮跑完发现一个口径问题：固定语料 `PILOT_TEXT` 只有 276 token，×4 也只有 **1104** token，
所以上表"2048"那一格实际喂的是 1104 token（已经作废）。为拿到真正的 2048 点，用
`PILOT_TEXT×8`（2208 token）重测 4 个臂 × 2 次重复（`p5_bench_2048.json`；`p5_parts_2048/` 保留逐次原始值）。

已测到的真 2048 前缀（2048 token，每次两遍）：

| 臂 | 第 1 次 prefill | 第 2 次 prefill | 折算 prefill 速率 |
| --- | --- | --- | --- |
| 精确档 fp16 | 398.9 s | 398.5 s | 5.13~5.14 tok/s |
| 均衡档 top8 | 345.1 s | 345.2 s | 5.91~5.94 tok/s |
| 快速档 top16 | 287.0 s | （收工时仍在跑） | 7.14 tok/s |
| 全量 int8 | 收工时仍在跑 | — | 预计 ~171 s（12 tok/s） |

**注意**：这三行的 prefill 速率与同臂的 decode 速率完全一致（5.13 vs 5.07 / 5.92 vs 6.02 /
7.14 vs 7.35），把 §4 第 1 条的结论从 1024 前缀外推到 2048 前缀依然成立：
**TTFT ≈ 前缀长度 ÷ decode 速率**。

## 6. 未做与局限

- **P95 / 尾延迟**：每个档位只有 5 次请求，按协议"样本不足时不声称可靠尾延迟"，只报原始值与中位数。
- **workspace 分解**：`.om` 是零拷贝 mmap、内核按需调页，没有把 ATC 编译时报告的 workspace 单独量出来。
- **能耗**：无可靠测量接口，留空。
- **前缀缓存开/关的对比**：本轮只测了"开缓存"（服务默认行为），关闭状态的对照需要改服务再跑一轮。
- **多并发**：服务是单线程（pyACL context 绑线程），本轮 batch=1；并发场景需要另做队列验证。
- **温度/功耗策略**：只记录观测温度（41~46°C），没有做调频实验。
